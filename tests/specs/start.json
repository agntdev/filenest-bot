[
  {
    "name": "/start replies with the welcome + main menu",
    "steps": [
      {
        "send": { "text": "/start" },
        "expect": [
          {
            "method": "sendMessage",
            "payload": { "text": "No library sections are available yet. Use Search when files are added." }
          }
        ]
      }
    ]
  }
]
